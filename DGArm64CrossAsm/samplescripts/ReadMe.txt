
Put diaperglu in the parent directory of this folder.
The parent directory should also have DGMachoDotoMaker.dglu and DGArm64CrossAsm.dglu
Then do:
 sudo make

This makes and runs the sample programs.

helloworld uses base words to build the things the .o file maker needs so you can see
  what is going on

simplehelloworld uses helper words build the things the .o file maker needs so you
  can see a simpler way to do things

       

     
                 
