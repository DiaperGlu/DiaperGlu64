
Put diaperglu in the parent directory of this folder.
The parent directory should also have DGMachoDotoMaker.dglu
Then do:
 sudo make

This makes 3 files and then runs the test.
Note: the makefile may run the test even if one of the compiles failed. If you scroll
  back to before the test output, you can see if a compile failed.
        
       

     
                 
